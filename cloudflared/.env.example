# Cloudflare Tunnel token (from Zero Trust → Networks → Tunnels → Configure)
# Copy to .env and fill in. Never commit .env.
#
# Get token: Cloudflare Dashboard → Zero Trust → Networks → Tunnels → [Your tunnel] → Configure → Install connector
#
TUNNEL_TOKEN=
